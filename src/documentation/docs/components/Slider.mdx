import Code from "@/components/internal/Documentation/Code";
import ExampleLoader from "@/documentation/utils/ExampleLoader";
import Meta from "@/components/internal/Documentation/Meta";
import PropsTable from "@/components/internal/Documentation/PropsTable";
import PropsTableRow from "@/components/internal/Documentation/PropsTable/components/PropsTableRow";
import SlotsTable from "@/components/internal/Documentation/SlotsTable";
import SlotsTableRow from "@/components/internal/Documentation/SlotsTable/components/SlotsTableRow";

<Meta
  title="Slider"
  subtitle="Displays elements as a slide-show"
/>

## Properties

<PropsTable>
  <PropsTableRow value="slides" type="SliderProps[]" required>
    An array of slides that should be shown (with each slide containing at least a 'render' method).
  </PropsTableRow>
 <PropsTableRow value="initialSlideIndex" type="number" default="0">
    Index of slide which should initially be displayed.
    Counting starts at one.
  </PropsTableRow>
 <PropsTableRow value="animateSlideTransition" type="boolean" default="false" >
   Specifies if the horizontal slide transition should be animated
  </PropsTableRow>
 <PropsTableRow value="visibleElements" type="number" default="1">
    Number of visible slides on one screen. 
  </PropsTableRow>
 <PropsTableRow value="infinite" type="boolean" default="false" >
   Specifies if the slider should start with the first slide again after reaching the last one.
  </PropsTableRow>
 <PropsTableRow value="animate" type="boolean" default="false" >
   Specifies if the slider should start automatically with the transition between slides so that no user-interaction is needed.
  </PropsTableRow>
  <PropsTableRow value="animationDelay" type="number" default="10000" >
    Specifies the delay between the automatic slide transition in ms.
    This only has an impact, if <strong>animate</strong> is set to true.
  </PropsTableRow>
</PropsTable>

## Slots

<SlotsTable>
  <SlotsTableRow value="controls" prop="RenderedType">
    The complete controls for the slider. The following props will be passed in:
    <ul>
      <li><span class="code">currentSlideIndex: number</span></li>
      <li><span class="code">nextSlideIndex: number</span></li>
      <li><span class="code">prevSlideIndex: number</span></li>
      <li><span class="code">showSlide(index): function</span></li>
    </ul>
  </SlotsTableRow>
</SlotsTable>
## Example

<ExampleLoader example="Slider.tsx" />